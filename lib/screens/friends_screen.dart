import 'package:flutter/material.dart';

class FriendsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Friends Page'),
      ),
      body: Center(
        child: Text('friends page####', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}